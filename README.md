# Unity Shader Tester

A simple 2D camera shader testing tool built in Unity. Users can drag in sprites or use premade sprites, select built-in shaders or import their own, then drag sprites through the scene to preview shader behavior.

---

## Use
For those who just want to use the tool:
- [Download the zip from the release page](https://github.com/Mykol-Word/unity-shader-tester/releases)
- Extract the zip
- Run `ShaderTester.exe`
- (Optional) Make a shortcut so you don't need to open the folder constantly



Otherwise, clone the repo and import the project into Unity.

---

## TODO
- Runtime shader compilation (currently shaders can only be nominally imported)
- Better resolution settings
- Shader descriptions
