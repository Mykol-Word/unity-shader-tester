# Unity Shader Tester

A simple 2D camera shader testing tool built in Unity. Users can drag in sprites or use premade sprites, select built-in shaders or import their own, then drag sprites through the scene to preview shader behavior.